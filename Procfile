web: cd WorkSpace/apps/api && npm install && npm run build && node dist/index.js
# bot process disabled for now to simplify Railway deploy (API-only). Re-enable by removing this comment line below.
# bot: cd WorkSpace/apps/bot && npm install && node dist/index.js
