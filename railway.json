{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd WorkSpace && npm install && npm run build -w packages/llm && npm run build -w packages/shared && npm run build -w packages/rules && npm run build -w packages/analytics && npm run build -w packages/security && npm run build -w packages/alerting && npm run build -w packages/workflows && npm run build -w apps/api"
  },
  "deploy": {
    "startCommand": "cd WorkSpace/apps/api && node dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
