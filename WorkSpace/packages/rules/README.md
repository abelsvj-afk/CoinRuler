# @coinruler/rules

Minimal rules engine scaffolding for CoinRuler.

- JSON-first RuleSpec with trigger/conditions/actions/risk
- Evaluator produces Intents for approvals or execution
- Risk layer enforces baseline protection and cooldowns

Future: shorthand DSL, indicators, optimizer, backtests.
