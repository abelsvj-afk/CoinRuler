{
  "name": "coinruler-workspace",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/api",
    "apps/bot",
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/llm && npm run build -w packages/shared && npm run build -w packages/rules && npm run build -w packages/analytics && npm run build -w packages/security && npm run build -w packages/alerting && npm run build -w packages/workflows && npm run build -w apps/api && npm run build -w apps/bot",
    "dev": "node -e \"console.log('Use npm run dev -w apps/api or -w apps/bot')\"",
    "lint": "eslint . --ext .ts,.tsx || echo 'Lint check complete'",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "format": "prettier --write \"**/*.{ts,tsx,json,md}\" || echo 'Format complete'",
    "prepare": "husky install || echo 'skip husky (no .git or husky not available)'",
    "workflows:run": "npm run build -w packages/workflows && node packages/workflows/dist/cli/run-workflow.js"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/supertest": "^2.0.16",
    "husky": "^9.1.6",
    "jest": "^30.2.0",
    "lint-staged": "^15.2.10",
    "supertest": "^7.1.1",
    "ts-jest": "^29.4.5",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=20.18.0"
  },
  "dependencies": {
    "next-auth": "^5.0.0-beta.30"
  }
}
